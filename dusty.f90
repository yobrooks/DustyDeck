program dusty
    implicit none
        INTEGER, PARAMETER :: MAXDIM = 5 !parameter==constant, real value is 100
        INTEGER, DIMENSION(MAXDIM) :: IA
        REAL(KIND = DP), DIMENSION(MAXDIM) :: AV, BV, CV
        REAL(KIND = DP), DIMENSION(MAXDIM, MAXDIM) :: OP, ID, AM, DM
        REAL
end program dusty
